# Normalize every text file to LF in the working tree. Without this, Windows
# checkouts get CRLF while git stores LF, so `biome check` reports a format
# error on all 23 files and the windows-latest CI jobs fail on code they never
# touched.
* text=auto eol=lf
